# Cloudflare Pages Redirects for SPA
# Place in public/ folder to be copied to dist/

# API routes - ensure they're not caught by SPA fallback
/functions/auth*  /functions/auth*  200

# SPA fallback - all other routes serve index.html
/*  /index.html  200